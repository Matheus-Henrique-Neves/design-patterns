class PagfacilAdapter extends pagfacil implements Gateway{
    //func()que já implementados no sistema
    setValor(valor: number): void {
        
    }
    setCVV(cvv: string): void {
        
    }
    
}